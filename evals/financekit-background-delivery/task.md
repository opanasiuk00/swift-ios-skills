# FinanceKit Background Widget Updates

## Problem

A team wants an iOS 26 widget to update spending totals when Wallet financial data changes in the background. Show the background delivery extension shape, registration calls, shared-storage requirements, and what should remain outside FinanceKit's core guidance.

## Output

Create a Markdown file named `financekit-background-delivery.md`.
