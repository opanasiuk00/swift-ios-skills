# FinanceKit Import Plan Review

## Problem

Review this import plan:

> Use `TransactionPicker` for expense reports, store the selected `Transaction` IDs forever, then use those IDs later to keep syncing updates. If users want full-budgeting sync, ask for all accounts and fetch complete snapshots every launch.

Provide a correction-focused review and a better approach for selective import versus ongoing budgeting sync. Keep the response focused on FinanceKit and FinanceKitUI.

## Output

Create a Markdown file named `financekit-picker-sync-boundary.md`.
