# visionOS Focus and Hover Boundary Review

## Problem

A team is building a visionOS RealityKit scene. Their plan says gaze should move SwiftUI focus between floating controls, and that `InputTargetComponent` plus `HoverEffectComponent` make 3D entities part of the focus system. They also want accessibility-specific focus guidance in the same answer.

Review the plan and correct the conceptual boundaries. Include enough implementation guidance to keep the RealityKit interaction path useful.

## Output

Create a Markdown file named `visionos-focus-hover-review.md`.
